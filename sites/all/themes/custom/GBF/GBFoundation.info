name = GBFoundation
description = Green Beret Foundation template for Drupal 6
version = 1.0
core = 6.x
engine = phptemplate

regions[slideshow] = Slideshow
regions[content] = Content
regions[sidebar] = Sidebar
regions[menu] = Menu

stylesheets[all][] = style.css
stylesheets[print][] = print.css

; add in links to .js files to include with this theme
; scripts[] = gbf.js


; These last few disabled by commenting out while leaving some present above
; (if none are present, all show)
; features[] = name
; features[] = logo
; features[] = slogan
; features[] = mission
  features[] = primary_links
; features[] = secondary_links